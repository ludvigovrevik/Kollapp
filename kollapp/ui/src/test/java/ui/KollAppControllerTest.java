package ui;

import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;
import java.util.List;

public class KollAppControllerTest {

    // Example test to check if Mockito is working
    @Test
    public void testListMock() {
        // Arrange

        //List<String> mockList = mock(List.class);  // Mock the List interface

        // Define behavior
        //when(mockList.size()).thenReturn(5);

        // Act
        //int size = mockList.size();

        // Assert
        //assertEquals(5, size);  // Check that the size is as expected
        //verify(mockList).size();  // Verify that the size method was called
    }
}
